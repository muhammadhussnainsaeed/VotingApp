import 'package:flutter/material.dart';


class ChangePinScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Change PIN'),
      ),
      body: Center(
        child: Text('Change PIN Screen'),
      ),
    );
  }
}